services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: battlefleet_server
    ports:
      - "8001:8000"
    environment:
      - BATTLEFLEET_SECRET_KEY=dev_secret_key_here
    volumes:
      - ./instance:/app/instance